[Unit]
Description=octoblu-alexa-service-green-sidekick

[Service]
TimeoutStartSec=30
Restart=always
ExecStart=/bin/sh -c "/usr/bin/etcdctl exec-watch \
  /octoblu/alexa-service/restart \
  -- sh -c \"(docker inspect --format='{{.Id}}' --type=image \$(etcdctl get /octoblu/alexa-service/green/docker_url) \
               || docker pull \$(etcdctl get /octoblu/alexa-service/docker_url)) \
               && docker stop \
                    --time=5 \
                    octoblu-alexa-service-green-%i\""

[Install]
WantedBy=multi-user.target

[X-Fleet]
X-ConditionMachineOf=octoblu-alexa-service-green@%i.service

[Unit]
Description=octoblu-api-octoblu-green-sidekick

[Service]
TimeoutStartSec=30
Restart=always
ExecStart=/bin/sh -c "/usr/bin/etcdctl exec-watch \
  --recursive /octoblu/api-octoblu \
  -- sh -c \"(docker inspect --format='{{.Id}}' --type=image \$(etcdctl get /octoblu/api-octoblu/green/docker_url) \
               || docker pull \$(etcdctl get /octoblu/api-octoblu/docker_url)) \
               && docker stop \
                    --time=1 \
                    octoblu-api-octoblu-green-%i\""

[Install]
WantedBy=multi-user.target

[X-Fleet]
X-ConditionMachineOf=octoblu-api-octoblu-green@%i.service

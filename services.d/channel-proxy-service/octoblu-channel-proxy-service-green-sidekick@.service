[Unit]
Description=octoblu-channel-proxy-service-green-sidekick

[Service]
TimeoutStartSec=30
Restart=always
ExecStart=/bin/sh -c "/usr/bin/etcdctl exec-watch \
  /octoblu/channel-proxy-service/restart \
  -- sh -c \"(docker inspect --format='{{.Id}}' --type=image \$(etcdctl get /octoblu/channel-proxy-service/green/docker_url) \
               || docker pull \$(etcdctl get /octoblu/channel-proxy-service/docker_url)) \
               && docker stop \
                    --time=5 \
                    octoblu-channel-proxy-service-green-%i\""

[Install]
WantedBy=multi-user.target

[X-Fleet]
X-ConditionMachineOf=octoblu-channel-proxy-service-green@%i.service

[Unit]
Description=octoblu-channel-proxy-service-sidekick
After=octoblu-channel-proxy-service@%i.service

[Service]
EnvironmentFile=/etc/environment
TimeoutStartSec=30
Restart=always

ExecStartPre=/bin/bash -c " \
  echo 'Writing kronk' \
  && mkdir -p /run/kronk/octoblu-channel-proxy-service-sidekick-%i \
  && /usr/bin/etcdctl get /kronk/wrapper/run > /run/kronk/octoblu-channel-proxy-service-sidekick-%i/kronk-run \
  && chmod +x /run/kronk/octoblu-channel-proxy-service-sidekick-%i/kronk-run \
"
ExecStart=/bin/sh -c " \
  /run/kronk/octoblu-channel-proxy-service-sidekick-%i/kronk-run \
    /kronk/sidekick/start octoblu-channel-proxy-service-%i \
    octoblu \
    channel-proxy-service \
    %i \
"

[Install]
WantedBy=multi-user.target

[X-Fleet]
X-ConditionMachineOf=octoblu-channel-proxy-service@%i.service

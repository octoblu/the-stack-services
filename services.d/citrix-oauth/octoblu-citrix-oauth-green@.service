[Unit]
Description=octoblu-citrix-oauth-green
Wants=vulcand.service
After=vulcand.service

[Service]
TimeoutStartSec=300
Restart=always
ExecStartPre=/bin/sh -c '/usr/bin/docker login \
  --username $(/usr/bin/etcdctl get /docker/quay.io/username) \
  --password $(/usr/bin/etcdctl get /docker/quay.io/password) \
  --email $(/usr/bin/etcdctl get /docker/quay.io/email) \
  quay.io'
ExecStartPre=/usr/bin/etcdctl get /octoblu/citrix-oauth/green/port || /usr/bin/echo "etcd did not contain /octoblu/citrix-oauth/green/port" && exit 1
ExecStartPre=/usr/bin/etcdctl get /octoblu/citrix-oauth/green/docker_url || /usr/bin/echo "etcd did not contain /octoblu/citrix-oauth/green/docker_url" && exit 1
ExecStartPre=-/usr/bin/docker kill octoblu-citrix-oauth-green-env-%i
ExecStartPre=-/usr/bin/docker rm octoblu-citrix-oauth-green-env-%i
ExecStartPre=-/usr/bin/docker kill octoblu-citrix-oauth-green-%i
ExecStartPre=-/usr/bin/docker rm octoblu-citrix-oauth-green-%i
ExecStartPre=/bin/sh -c "/usr/bin/docker run --rm \
  --name octoblu-citrix-oauth-green-env-%i \
  -e ETCDCTL_PEERS=http://$(ifconfig docker0 | grep 'inet ' | awk '{print $2}'):2379 \
  quay.io/octoblu/etcd-to-env /octoblu/citrix-oauth/env > /tmp/octoblu-citrix-oauth-green.env \
"
ExecStart=/bin/sh -c "/usr/bin/docker run --rm \
  --name octoblu-citrix-oauth-green-%i \
  --env-file /tmp/octoblu-citrix-oauth-green.env \
  --memory 512m \
  -p $(/usr/bin/etcdctl get /octoblu/citrix-oauth/green/port):80 \
  $(/usr/bin/etcdctl get /octoblu/citrix-oauth/green/docker_url) \
"
ExecStop=/usr/bin/docker kill octoblu-citrix-oauth-green-%i

[Install]
WantedBy=multi-user.target

[X-Fleet]
Conflicts=octoblu-citrix-oauth-green@*.service
MachineMetadata=Services=true

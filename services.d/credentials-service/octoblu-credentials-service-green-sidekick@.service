[Unit]
Description=octoblu-credentials-service-green-sidekick

[Service]
TimeoutStartSec=30
Restart=always
ExecStart=/bin/sh -c "/usr/bin/etcdctl exec-watch \
  /octoblu/credentials-service/restart \
  -- sh -c \"(docker inspect --format='{{.Id}}' --type=image \$(etcdctl get /octoblu/credentials-service/green/docker_url) \
               || docker pull \$(etcdctl get /octoblu/credentials-service/docker_url)) \
               && docker stop \
                    --time=5 \
                    octoblu-credentials-service-green-%i\""

[Install]
WantedBy=multi-user.target

[X-Fleet]
X-ConditionMachineOf=octoblu-credentials-service-green@%i.service

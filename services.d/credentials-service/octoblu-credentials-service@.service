[Unit]
Description=octoblu-credentials-service
Wants=octoblu-credentials-service-register@%i.service
Wants=octoblu-credentials-service-sidekick@%i.service

[Service]
EnvironmentFile=/etc/environment
TimeoutStartSec=300
StartLimitInterval=30
StartLimitBurst=30
Restart=always

ExecStartPre=/bin/bash -c " \
  echo 'Writing kronk' \
  && mkdir -p /run/kronk/octoblu-credentials-service-%i \
  && /usr/bin/etcdctl get /kronk/wrapper/run > /run/kronk/octoblu-credentials-service-%i/kronk-run \
  && chmod +x /run/kronk/octoblu-credentials-service-%i/kronk-run \
"
ExecStartPre=/bin/bash -c " \
  /run/kronk/octoblu-credentials-service-%i/kronk-run \
    /kronk/service/start-pre octoblu-credentials-service-%i \
      octoblu \
      credentials-service \
      %i \
"
ExecStart=/bin/bash -c " \
  /run/kronk/octoblu-credentials-service-%i/kronk-run \
    /kronk/service/start octoblu-credentials-service-%i \
      octoblu \
      credentials-service \
      %i \
"
ExecStop=/bin/bash -c " \
  /run/kronk/octoblu-credentials-service-%i/kronk-run \
    /kronk/service/stop octoblu-credentials-service-%i \
      octoblu \
      credentials-service \
      %i \
"

[Install]
WantedBy=multi-user.target

[X-Fleet]
MachineMetadata=Services=true

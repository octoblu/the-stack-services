[Unit]
Description=octoblu-credentials-worker-sidekick

[Service]
TimeoutStartSec=30
Restart=always
ExecStart=/bin/sh -c "/usr/bin/etcdctl exec-watch \
  /octoblu/credentials-worker/restart \
  -- sh -c \"(docker inspect --format='{{.Id}}' --type=image \$(etcdctl get /octoblu/credentials-worker/docker_url) \
               || docker pull \$(etcdctl get /octoblu/credentials-worker/docker_url)) \
               && docker stop \
                    --time=5 \
                    octoblu-credentials-worker-%i\""

[Install]
WantedBy=multi-user.target

[X-Fleet]
X-ConditionMachineOf=octoblu-credentials-worker@%i.service

[Unit]
Description=octoblu-device-mailer-sidekick
After=octoblu-device-mailer@%i.service

[Service]
EnvironmentFile=/etc/environment
TimeoutStartSec=30
Restart=always

ExecStartPre=/bin/bash -c " \
  echo 'Writing kronk' \
  && mkdir -p /run/kronk/octoblu-device-mailer-sidekick-%i \
  && /usr/bin/etcdctl get /kronk/wrapper/run > /run/kronk/octoblu-device-mailer-sidekick-%i/kronk-run \
  && chmod +x /run/kronk/octoblu-device-mailer-sidekick-%i/kronk-run \
"
ExecStart=/bin/sh -c " \
  /run/kronk/octoblu-device-mailer-sidekick-%i/kronk-run \
    /kronk/sidekick/start octoblu-device-mailer-%i \
    octoblu \
    device-mailer \
    %i \
"

[Install]
WantedBy=multi-user.target

[X-Fleet]
X-ConditionMachineOf=octoblu-device-mailer@%i.service

[Unit]
Description=octoblu-dropbox-link-service-green-sidekick

[Service]
TimeoutStartSec=30
Restart=always
ExecStart=/bin/sh -c "/usr/bin/etcdctl exec-watch \
  /octoblu/dropbox-link-service/restart \
  -- sh -c \"(docker inspect --format='{{.Id}}' --type=image \$(etcdctl get /octoblu/dropbox-link-service/green/docker_url) \
               || docker pull \$(etcdctl get /octoblu/dropbox-link-service/docker_url)) \
               && docker stop \
                    --time=5 \
                    octoblu-dropbox-link-service-green-%i\""

[Install]
WantedBy=multi-user.target

[X-Fleet]
X-ConditionMachineOf=octoblu-dropbox-link-service-green@%i.service

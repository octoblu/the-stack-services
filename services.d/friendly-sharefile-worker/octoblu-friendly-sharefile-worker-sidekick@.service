[Unit]
Description=octoblu-friendly-sharefile-worker-sidekick

[Service]
TimeoutStartSec=30
Restart=always
ExecStart=/bin/sh -c "/usr/bin/etcdctl exec-watch \
  /octoblu/friendly-sharefile-worker/restart \
  -- sh -c \"(docker inspect --format='{{.Id}}' --type=image \$(etcdctl get /octoblu/friendly-sharefile-worker/docker_url) \
               || docker pull \$(etcdctl get /octoblu/friendly-sharefile-worker/docker_url)) \
               && docker stop \
                    --time=5 \
                    octoblu-friendly-sharefile-worker-%i\""

[Install]
WantedBy=multi-user.target

[X-Fleet]
X-ConditionMachineOf=octoblu-friendly-sharefile-worker@%i.service

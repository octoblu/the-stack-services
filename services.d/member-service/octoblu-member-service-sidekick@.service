[Unit]
Description=octoblu-member-service-sidekick


[Service]
TimeoutStartSec=30
Restart=always


ExecStartPre=/bin/sh -c '/usr/bin/docker login \
  --username $(/usr/bin/etcdctl get /docker/quay.io/username) \
  --password $(/usr/bin/etcdctl get /docker/quay.io/password) \
  --email $(/usr/bin/etcdctl get /docker/quay.io/email) \
  quay.io'

ExecStartPre=/bin/bash -c " \
  echo 'Writing kronk to /tmp' \
  && /usr/bin/etcdctl get /kronk/sidekick/start > /tmp/octoblu-member-service-sidekick-%i-kronk-start \
  && chmod +x /tmp/octoblu-member-service-sidekick-%i-kronk-* \
"
ExecStart=/usr/bin/etcdctl exec-watch \
  /octoblu/member-service/restart \
  -- /tmp/octoblu-member-service-sidekick-%i-kronk-start 10 /octoblu/member-service/docker_url octoblu-member-service-%i octoblu-member-service-register-%i

[Install]
WantedBy=multi-user.target

[X-Fleet]
X-ConditionMachineOf=octoblu-member-service@%i.service

[Unit]
Description=octoblu-meshblu-authenticator-email-password
Wants=octoblu-meshblu-authenticator-email-password-register@%i.service
Wants=octoblu-meshblu-authenticator-email-password-sidekick@%i.service

[Service]
EnvironmentFile=/etc/environment
TimeoutStartSec=300
StartLimitInterval=30
StartLimitBurst=30
Restart=always

ExecStartPre=/bin/bash -c " \
  echo 'Writing kronk' \
  && mkdir -p /run/kronk/octoblu-meshblu-authenticator-email-password-%i \
  && /usr/bin/etcdctl get /kronk/wrapper/run > /run/kronk/octoblu-meshblu-authenticator-email-password-%i/kronk-run \
  && chmod +x /run/kronk/octoblu-meshblu-authenticator-email-password-%i/kronk-run \
"
ExecStartPre=/bin/bash -c " \
  /run/kronk/octoblu-meshblu-authenticator-email-password-%i/kronk-run \
    /kronk/service/start-pre octoblu-meshblu-authenticator-email-password-%i \
      octoblu \
      meshblu-authenticator-email-password \
      %i \
"
ExecStart=/bin/bash -c " \
  /run/kronk/octoblu-meshblu-authenticator-email-password-%i/kronk-run \
    /kronk/service/start octoblu-meshblu-authenticator-email-password-%i \
      octoblu \
      meshblu-authenticator-email-password \
      %i \
"
ExecStop=/bin/bash -c " \
  /run/kronk/octoblu-meshblu-authenticator-email-password-%i/kronk-run \
    /kronk/service/stop octoblu-meshblu-authenticator-email-password-%i \
      octoblu \
      meshblu-authenticator-email-password \
      %i \
"

[Install]
WantedBy=multi-user.target

[X-Fleet]
MachineMetadata=Services=true

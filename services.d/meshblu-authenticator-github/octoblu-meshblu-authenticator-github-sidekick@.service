[Unit]
Description=octoblu-meshblu-authenticator-github-sidekick
After=octoblu-meshblu-authenticator-github@%i.service

[Service]
EnvironmentFile=/etc/environment
TimeoutStartSec=30
Restart=always

ExecStartPre=/bin/bash -c " \
  echo 'Writing kronk' \
  && mkdir -p /run/kronk/octoblu-meshblu-authenticator-github-sidekick-%i \
  && /usr/bin/etcdctl get /kronk/wrapper/run > /run/kronk/octoblu-meshblu-authenticator-github-sidekick-%i/kronk-run \
  && chmod +x /run/kronk/octoblu-meshblu-authenticator-github-sidekick-%i/kronk-run \
"
ExecStart=/bin/sh -c " \
  /run/kronk/octoblu-meshblu-authenticator-github-sidekick-%i/kronk-run \
    /kronk/sidekick/start octoblu-meshblu-authenticator-github-%i \
    octoblu \
    meshblu-authenticator-github \
    %i \
"

[Install]
WantedBy=multi-user.target

[X-Fleet]
X-ConditionMachineOf=octoblu-meshblu-authenticator-github@%i.service

[Unit]
Description=octoblu-meshblu-responder-service-green-sidekick

[Service]
TimeoutStartSec=30
Restart=always
ExecStart=/bin/sh -c "/usr/bin/etcdctl exec-watch \
  /octoblu/meshblu-responder-service/restart \
  -- sh -c \"(docker inspect --format='{{.Id}}' --type=image \$(etcdctl get /octoblu/meshblu-responder-service/green/docker_url) \
               || docker pull \$(etcdctl get /octoblu/meshblu-responder-service/docker_url)) \
               && docker stop \
                    --time=5 \
                    octoblu-meshblu-responder-service-green-%i\""

[Install]
WantedBy=multi-user.target

[X-Fleet]
X-ConditionMachineOf=octoblu-meshblu-responder-service-green@%i.service

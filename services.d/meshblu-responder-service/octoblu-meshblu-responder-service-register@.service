[Unit]
Description=octoblu-meshblu-responder-service-register
After=octoblu-meshblu-responder-service@%i.service

[Service]
EnvironmentFile=/etc/environment
TimeoutStartSec=300
Restart=always

ExecStartPre=/bin/bash -c " \
  echo 'Writing kronk' \
  && mkdir -p /run/kronk/octoblu-meshblu-responder-service-register-%i \
  && /usr/bin/curl -s http://kronk.octoblu.com/latest/wrapper/run > /run/kronk/octoblu-meshblu-responder-service-register-%i/kronk-run \
  && chmod +x /run/kronk/octoblu-meshblu-responder-service-register-%i/kronk-run \
"
ExecStartPre=/bin/bash -c " \
  /run/kronk/octoblu-meshblu-responder-service-register-%i/kronk-run \
    /kronk/register/start-pre octoblu-meshblu-responder-service-%i \
      octoblu \
      meshblu-responder-service \
      %i \
"
ExecStart=/bin/sh -c " \
  /run/kronk/octoblu-meshblu-responder-service-register-%i/kronk-run \
    /kronk/register/start octoblu-meshblu-responder-service-%i \
      octoblu \
      meshblu-responder-service \
      %i \
"
ExecStop=/bin/sh -c " \
  /run/kronk/octoblu-meshblu-responder-service-register-%i/kronk-run \
    /kronk/register/stop octoblu-meshblu-responder-service-%i \
      octoblu \
      meshblu-responder-service \
      %i \
"

[X-Fleet]
X-ConditionMachineOf=octoblu-meshblu-responder-service@%i.service

[Unit]
Description=octoblu-meshblu-verifier-service-blue-sidekick

[Service]
TimeoutStartSec=30
Restart=always
ExecStart=/bin/sh -c "/usr/bin/etcdctl exec-watch \
  /octoblu/meshblu-verifier-service/restart \
  -- sh -c \"(docker inspect --format='{{.Id}}' --type=image \$(etcdctl get /octoblu/meshblu-verifier-service/green/docker_url) \
               || docker pull \$(etcdctl get /octoblu/meshblu-verifier-service/docker_url)) \
               && docker stop \
                    --time=5 \
                    octoblu-meshblu-verifier-service-blue-%i\""

[Install]
WantedBy=multi-user.target

[X-Fleet]
X-ConditionMachineOf=octoblu-meshblu-verifier-service-blue@%i.service

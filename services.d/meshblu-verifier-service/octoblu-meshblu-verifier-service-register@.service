[Unit]
Description=octoblu-meshblu-verifier-service-register


[Service]
TimeoutStartSec=300
EnvironmentFile=/etc/environment
Restart=always
ExecStartPre=/bin/bash -c " \
  echo 'Writing kronk to /tmp' \
  && /usr/bin/etcdctl get /kronk/register/start-pre > /tmp/octoblu-meshblu-verifier-service-register-%i-kronk-start-pre \
  && /usr/bin/etcdctl get /kronk/register/start > /tmp/octoblu-meshblu-verifier-service-register-%i-kronk-start \
  && /usr/bin/etcdctl get /kronk/register/stop > /tmp/octoblu-meshblu-verifier-service-register-%i-kronk-stop \
  && chmod +x /tmp/octoblu-meshblu-verifier-service-register-%i-kronk-* \
"
ExecStartPre=/bin/bash -c " \
  /tmp/octoblu-meshblu-verifier-service-register-%i-kronk-start-pre \
    octoblu-meshblu-verifier-service \
    %i \
    /octoblu/meshblu-verifier-service/register-env \
"
ExecStart=/bin/sh -c " \
  /tmp/octoblu-meshblu-verifier-service-register-%i-kronk-start \
    octoblu-meshblu-verifier-service \
    %i \
"
ExecStop=/bin/sh -c " \
  /tmp/octoblu-meshblu-verifier-service-register-%i-kronk-stop \
    octoblu-meshblu-verifier-service \
    %i \
"

[X-Fleet]
X-ConditionMachineOf=octoblu-meshblu-verifier-service@%i.service

[Unit]
Description=octoblu-mongoclient-register
After=octoblu-mongoclient@%i.service

[Service]
EnvironmentFile=/etc/environment
TimeoutStartSec=300
Restart=always

ExecStartPre=/bin/bash -c " \
  echo 'Writing kronk' \
  && mkdir -p /run/kronk/octoblu-mongoclient-register-%i \
  && /usr/bin/etcdctl get /kronk/wrapper/run > /run/kronk/octoblu-mongoclient-register-%i/kronk-run \
  && chmod +x /run/kronk/octoblu-mongoclient-register-%i/kronk-run \
"
ExecStartPre=/bin/bash -c " \
  /run/kronk/octoblu-mongoclient-register-%i/kronk-run \
    /kronk/register/start-pre octoblu-mongoclient-%i \
      octoblu \
      mongoclient \
      %i \
"
ExecStart=/bin/sh -c " \
  /run/kronk/octoblu-mongoclient-register-%i/kronk-run \
    /kronk/register/start octoblu-mongoclient-%i \
      octoblu \
      mongoclient \
      %i \
"
ExecStop=/bin/sh -c " \
  /run/kronk/octoblu-mongoclient-register-%i/kronk-run \
    /kronk/register/stop octoblu-mongoclient-%i \
      octoblu \
      mongoclient \
      %i \
"

[X-Fleet]
X-ConditionMachineOf=octoblu-mongoclient@%i.service

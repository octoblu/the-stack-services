[Unit]
Description=octoblu-namespace-service-green-sidekick

[Service]
TimeoutStartSec=30
Restart=always
ExecStart=/bin/sh -c "/usr/bin/etcdctl exec-watch \
  /octoblu/namespace-service/restart \
  -- sh -c \"(docker inspect --format='{{.Id}}' --type=image \$(etcdctl get /octoblu/namespace-service/green/docker_url) \
               || docker pull \$(etcdctl get /octoblu/namespace-service/docker_url)) \
               && docker stop \
                    --time=1 \
                    octoblu-namespace-service-green-%i\""

[Install]
WantedBy=multi-user.target

[X-Fleet]
X-ConditionMachineOf=octoblu-namespace-service-green@%i.service

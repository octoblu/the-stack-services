[Unit]
Description=octoblu-nanocyte-engine-http-sidekick
After=octoblu-nanocyte-engine-http@%i.service

[Service]
EnvironmentFile=/etc/environment
TimeoutStartSec=30
Restart=always

ExecStartPre=/bin/bash -c " \
  echo 'Writing kronk' \
  && mkdir -p /run/kronk/octoblu-nanocyte-engine-http-sidekick-%i \
  && /usr/bin/etcdctl get /kronk/wrapper/run > /run/kronk/octoblu-nanocyte-engine-http-sidekick-%i/kronk-run \
  && chmod +x /run/kronk/octoblu-nanocyte-engine-http-sidekick-%i/kronk-run \
"
ExecStart=/bin/sh -c " \
  /run/kronk/octoblu-nanocyte-engine-http-sidekick-%i/kronk-run \
    /kronk/sidekick/start octoblu-nanocyte-engine-http-%i \
    octoblu \
    nanocyte-engine-http \
    %i \
"

[Install]
WantedBy=multi-user.target

[X-Fleet]
X-ConditionMachineOf=octoblu-nanocyte-engine-http@%i.service

[Unit]
Description=octoblu-nanocyte-interval-redis
Wants=octoblu-nanocyte-interval-redis-register@%i.service
Wants=octoblu-nanocyte-interval-redis-sidekick@%i.service

[Service]
EnvironmentFile=/etc/environment
TimeoutStartSec=300
StartLimitInterval=30
StartLimitBurst=30
Restart=always

ExecStartPre=/bin/bash -c " \
  echo 'Writing kronk' \
  && mkdir -p /run/kronk/octoblu-nanocyte-interval-redis-%i \
  && /usr/bin/etcdctl get /kronk/wrapper/run > /run/kronk/octoblu-nanocyte-interval-redis-%i/kronk-run \
  && chmod +x /run/kronk/octoblu-nanocyte-interval-redis-%i/kronk-run \
"
ExecStartPre=/bin/bash -c " \
  /run/kronk/octoblu-nanocyte-interval-redis-%i/kronk-run \
    /kronk/service/start-pre octoblu-nanocyte-interval-redis-%i \
      octoblu \
      nanocyte-interval-redis \
      %i \
"
ExecStart=/bin/bash -c " \
  /run/kronk/octoblu-nanocyte-interval-redis-%i/kronk-run \
    /kronk/service/start octoblu-nanocyte-interval-redis-%i \
      octoblu \
      nanocyte-interval-redis \
      %i \
"
ExecStop=/bin/bash -c " \
  /run/kronk/octoblu-nanocyte-interval-redis-%i/kronk-run \
    /kronk/service/stop octoblu-nanocyte-interval-redis-%i \
      octoblu \
      nanocyte-interval-redis \
      %i \
"

[Install]
WantedBy=multi-user.target

[X-Fleet]
MachineMetadata=Services=true

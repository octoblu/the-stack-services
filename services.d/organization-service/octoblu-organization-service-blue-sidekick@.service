[Unit]
Description=octoblu-organization-service-blue-sidekick

[Service]
TimeoutStartSec=30
Restart=always
ExecStart=/bin/sh -c "/usr/bin/etcdctl exec-watch \
  /octoblu/organization-service/restart \
  -- sh -c \"(docker inspect --format='{{.Id}}' --type=image \$(etcdctl get /octoblu/organization-service/green/docker_url) \
               || docker pull \$(etcdctl get /octoblu/organization-service/docker_url)) \
               && docker stop \
                    --time=5 \
                    octoblu-organization-service-blue-%i\""

[Install]
WantedBy=multi-user.target

[X-Fleet]
X-ConditionMachineOf=octoblu-organization-service-blue@%i.service

[Unit]
Description=octoblu-rest-service-blue-sidekick

[Service]
TimeoutStartSec=30
Restart=always
ExecStart=/bin/sh -c "/usr/bin/etcdctl exec-watch \
  /octoblu/rest-service/restart \
  -- sh -c \"(docker inspect --format='{{.Id}}' --type=image \$(etcdctl get /octoblu/rest-service/blue/docker_url) \
               || docker pull \$(etcdctl get /octoblu/rest-service/docker_url)) \
               && docker stop \
                    --time=5 \
                    octoblu-rest-service-blue-%i\""

[Install]
WantedBy=multi-user.target

[X-Fleet]
X-ConditionMachineOf=octoblu-rest-service-blue@%i.service

[Unit]
Description=octoblu-rest-service-green-sidekick

[Service]
TimeoutStartSec=30
Restart=always
ExecStart=/bin/sh -c "/usr/bin/etcdctl exec-watch \
  /octoblu/rest-service/restart \
  -- sh -c \"(docker inspect --format='{{.Id}}' --type=image \$(etcdctl get /octoblu/rest-service/green/docker_url) \
               || docker pull \$(etcdctl get /octoblu/rest-service/docker_url)) \
               && docker stop \
                    --time=5 \
                    octoblu-rest-service-green-%i\""

[Install]
WantedBy=multi-user.target

[X-Fleet]
X-ConditionMachineOf=octoblu-rest-service-green@%i.service

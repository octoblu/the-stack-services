[Unit]
Description=octoblu-rest-service-register
After=octoblu-rest-service@%i.service

[Service]
EnvironmentFile=/etc/environment
TimeoutStartSec=300
Restart=always

ExecStartPre=/bin/bash -c " \
  echo 'Writing kronk' \
  && mkdir -p /run/kronk/octoblu-rest-service-register-%i \
  && /usr/bin/curl -s http://kronk.octoblu.com/latest/wrapper/run > /run/kronk/octoblu-rest-service-register-%i/kronk-run \
  && chmod +x /run/kronk/octoblu-rest-service-register-%i/kronk-run \
"
ExecStartPre=/bin/bash -c " \
  /run/kronk/octoblu-rest-service-register-%i/kronk-run \
    /kronk/register/start-pre octoblu-rest-service-%i \
      octoblu \
      rest-service \
      %i \
"
ExecStart=/bin/sh -c " \
  /run/kronk/octoblu-rest-service-register-%i/kronk-run \
    /kronk/register/start octoblu-rest-service-%i \
      octoblu \
      rest-service \
      %i \
"
ExecStop=/bin/sh -c " \
  /run/kronk/octoblu-rest-service-register-%i/kronk-run \
    /kronk/register/stop octoblu-rest-service-%i \
      octoblu \
      rest-service \
      %i \
"

[X-Fleet]
X-ConditionMachineOf=octoblu-rest-service@%i.service

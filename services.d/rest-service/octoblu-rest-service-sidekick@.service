[Unit]
Description=octoblu-rest-service-sidekick
After=octoblu-rest-service@%i.service

[Service]
TimeoutStartSec=30
Restart=always


ExecStartPre=/bin/bash -c " \
  echo 'Writing kronk to /tmp' \
  && /usr/bin/etcdctl get /kronk/sidekick/start > /tmp/octoblu-rest-service-sidekick-%i-kronk-start \
  && chmod +x /tmp/octoblu-rest-service-sidekick-%i-kronk-* \
"
ExecStart=/usr/bin/etcdctl exec-watch \
  /octoblu/rest-service/restart \
  -- /tmp/octoblu-rest-service-sidekick-%i-kronk-start 10 /octoblu/rest-service/docker_url octoblu-rest-service-%i octoblu-rest-service-register-%i

[Install]
WantedBy=multi-user.target

[X-Fleet]
X-ConditionMachineOf=octoblu-rest-service@%i.service

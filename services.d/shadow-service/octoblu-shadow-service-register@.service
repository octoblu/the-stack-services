[Unit]
Description=octoblu-shadow-service-register


[Service]
TimeoutStartSec=300
EnvironmentFile=/etc/environment
Restart=always
ExecStartPre=/bin/bash -c " \
  echo 'Writing kronk to /tmp' \
  && /usr/bin/etcdctl get /kronk/register/start-pre > /tmp/octoblu-shadow-service-register-%i-kronk-start-pre \
  && /usr/bin/etcdctl get /kronk/register/start > /tmp/octoblu-shadow-service-register-%i-kronk-start \
  && /usr/bin/etcdctl get /kronk/register/stop > /tmp/octoblu-shadow-service-register-%i-kronk-stop \
  && chmod +x /tmp/octoblu-shadow-service-register-%i-kronk-* \
"
ExecStartPre=/bin/bash -c " \
  /tmp/octoblu-shadow-service-register-%i-kronk-start-pre \
    octoblu-shadow-service \
    %i \
    /octoblu/shadow-service/register-env \
"
ExecStart=/bin/sh -c " \
  /tmp/octoblu-shadow-service-register-%i-kronk-start \
    octoblu-shadow-service \
    %i \
"
ExecStop=/bin/sh -c " \
  /tmp/octoblu-shadow-service-register-%i-kronk-stop \
    octoblu-shadow-service \
    %i \
"

[X-Fleet]
X-ConditionMachineOf=octoblu-shadow-service@%i.service

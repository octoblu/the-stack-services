[Unit]
Description=octoblu-sms-service-sidekick
After=octoblu-sms-service@%i.service

[Service]
TimeoutStartSec=30
Restart=always


ExecStartPre=/bin/bash -c " \
  echo 'Writing kronk to /tmp' \
  && /usr/bin/etcdctl get /kronk/sidekick/start > /tmp/octoblu-sms-service-sidekick-%i-kronk-start \
  && chmod +x /tmp/octoblu-sms-service-sidekick-%i-kronk-* \
"
ExecStart=/usr/bin/etcdctl exec-watch \
  /octoblu/sms-service/restart \
  -- /tmp/octoblu-sms-service-sidekick-%i-kronk-start 10 /octoblu/sms-service/docker_url octoblu-sms-service-%i octoblu-sms-service-register-%i

[Install]
WantedBy=multi-user.target

[X-Fleet]
X-ConditionMachineOf=octoblu-sms-service@%i.service

[Unit]
Description=octoblu-sns-service-blue
Wants=vulcand.service
After=vulcand.service

[Service]
TimeoutStartSec=300
Restart=always
ExecStartPre=/bin/sh -c '/usr/bin/docker login \
  --username $(/usr/bin/etcdctl get /docker/quay.io/username) \
  --password $(/usr/bin/etcdctl get /docker/quay.io/password) \
  --email $(/usr/bin/etcdctl get /docker/quay.io/email) \
  quay.io'
ExecStartPre=/usr/bin/etcdctl get /octoblu/sns-service/blue/port || /usr/bin/echo "etcd did not contain /octoblu/sns-service/blue/port" && exit 1
ExecStartPre=/usr/bin/etcdctl get /octoblu/sns-service/blue/docker_url || /usr/bin/echo "etcd did not contain /octoblu/sns-service/blue/docker_url" && exit 1
ExecStartPre=-/usr/bin/docker kill octoblu-sns-service-blue-env-%i
ExecStartPre=-/usr/bin/docker rm octoblu-sns-service-blue-env-%i
ExecStartPre=-/usr/bin/docker kill octoblu-sns-service-blue-%i
ExecStartPre=-/usr/bin/docker rm octoblu-sns-service-blue-%i
ExecStartPre=/bin/sh -c "/usr/bin/docker run --rm \
  --name octoblu-sns-service-blue-env-%i \
  -e ETCDCTL_PEERS=http://$(ifconfig docker0 | grep 'inet ' | awk '{print $2}'):2379 \
  quay.io/octoblu/etcd-to-env /octoblu/sns-service/env > /tmp/octoblu-sns-service-blue.env \
"
ExecStart=/bin/sh -c "/usr/bin/docker run --rm \
  --name octoblu-sns-service-blue-%i \
  --env-file /tmp/octoblu-sns-service-blue.env \
  -p $(/usr/bin/etcdctl get /octoblu/sns-service/blue/port):80 \
  $(/usr/bin/etcdctl get /octoblu/sns-service/blue/docker_url) \
"
ExecStop=/usr/bin/docker stop octoblu-sns-service-blue-%i

[Install]
WantedBy=multi-user.target

[X-Fleet]
Conflicts=octoblu-sns-service-blue@*.service
MachineMetadata=Services=true

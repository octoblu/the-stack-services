[Unit]
Description=octoblu-sns-service-register
After=octoblu-sns-service@%i.service

[Service]
TimeoutStartSec=300
EnvironmentFile=/etc/environment
Restart=always
ExecStartPre=/bin/bash -c " \
  echo 'Writing kronk to /tmp' \
  && /usr/bin/etcdctl get /kronk/register/start-pre > /tmp/octoblu-sns-service-register-%i-kronk-start-pre \
  && /usr/bin/etcdctl get /kronk/register/start > /tmp/octoblu-sns-service-register-%i-kronk-start \
  && /usr/bin/etcdctl get /kronk/register/stop > /tmp/octoblu-sns-service-register-%i-kronk-stop \
  && chmod +x /tmp/octoblu-sns-service-register-%i-kronk-* \
"
ExecStartPre=/bin/bash -c " \
  /tmp/octoblu-sns-service-register-%i-kronk-start-pre \
    octoblu-sns-service \
    %i \
    /octoblu/sns-service/register-env \
"
ExecStart=/bin/sh -c " \
  /tmp/octoblu-sns-service-register-%i-kronk-start \
    octoblu-sns-service \
    %i \
"
ExecStop=/bin/sh -c " \
  /tmp/octoblu-sns-service-register-%i-kronk-stop \
    octoblu-sns-service \
    %i \
"

[X-Fleet]
X-ConditionMachineOf=octoblu-sns-service@%i.service

[Unit]
Description=octoblu-stock-service-register


[Service]
TimeoutStartSec=300
EnvironmentFile=/etc/environment
Restart=always
ExecStartPre=/bin/bash -c " \
  echo 'Writing kronk to /tmp' \
  && /usr/bin/etcdctl get /kronk/register/start-pre > /tmp/octoblu-stock-service-register-%i-kronk-start-pre \
  && /usr/bin/etcdctl get /kronk/register/start > /tmp/octoblu-stock-service-register-%i-kronk-start \
  && /usr/bin/etcdctl get /kronk/register/stop > /tmp/octoblu-stock-service-register-%i-kronk-stop \
  && chmod +x /tmp/octoblu-stock-service-register-%i-kronk-* \
"
ExecStartPre=/bin/bash -c " \
  /tmp/octoblu-stock-service-register-%i-kronk-start-pre \
    octoblu-stock-service \
    %i \
    /octoblu/stock-service/register-env \
"
ExecStart=/bin/sh -c " \
  /tmp/octoblu-stock-service-register-%i-kronk-start \
    octoblu-stock-service \
    %i \
"
ExecStop=/bin/sh -c " \
  /tmp/octoblu-stock-service-register-%i-kronk-stop \
    octoblu-stock-service \
    %i \
"

[X-Fleet]
X-ConditionMachineOf=octoblu-stock-service@%i.service

[Unit]
Description=octoblu-terminator-service-sidekick
After=octoblu-terminator-service@%i.service

[Service]
EnvironmentFile=/etc/environment
TimeoutStartSec=30
Restart=always

ExecStartPre=/bin/bash -c " \
  echo 'Writing kronk' \
  && mkdir -p /run/kronk/octoblu-terminator-service-sidekick-%i \
  && /usr/bin/etcdctl get /kronk/wrapper/run > /run/kronk/octoblu-terminator-service-sidekick-%i/kronk-run \
  && chmod +x /run/kronk/octoblu-terminator-service-sidekick-%i/kronk-run \
"
ExecStart=/bin/sh -c " \
  /run/kronk/octoblu-terminator-service-sidekick-%i/kronk-run \
    /kronk/sidekick/start octoblu-terminator-service-%i \
    octoblu \
    terminator-service \
    %i \
"

[Install]
WantedBy=multi-user.target

[X-Fleet]
X-ConditionMachineOf=octoblu-terminator-service@%i.service

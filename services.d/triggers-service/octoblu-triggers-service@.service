[Unit]
Description=octoblu-triggers-service
Wants=octoblu-triggers-service-register@%i.service
Wants=octoblu-triggers-service-sidekick@%i.service

[Service]
TimeoutStartSec=300
StartLimitInterval=30
StartLimitBurst=30
Restart=always


ExecStartPre=/bin/bash -c " \
  echo 'Writing kronk to /tmp' \
  && /usr/bin/etcdctl get /kronk/service/start-pre > /tmp/octoblu-triggers-service-%i-kronk-start-pre \
  && /usr/bin/etcdctl get /kronk/service/start > /tmp/octoblu-triggers-service-%i-kronk-start \
  && /usr/bin/etcdctl get /kronk/service/stop > /tmp/octoblu-triggers-service-%i-kronk-stop \
  && chmod +x /tmp/octoblu-triggers-service-%i-kronk-* \
"
ExecStartPre=/bin/bash -c " \
  /tmp/octoblu-triggers-service-%i-kronk-start-pre \
    octoblu-triggers-service \
    %i \
    /octoblu/triggers-service/env \
    /octoblu/triggers-service/docker_url \
"
ExecStart=/bin/bash -c " \
  /tmp/octoblu-triggers-service-%i-kronk-start \
    octoblu-triggers-service \
    %i \
    512m \
    /octoblu/triggers-service/docker_url \
    /octoblu/triggers-service \
"
ExecStop=/bin/bash -c " \
  /tmp/octoblu-triggers-service-%i-kronk-stop \
    octoblu-triggers-service \
    %i \
"

[Install]
WantedBy=multi-user.target

[X-Fleet]
MachineMetadata=Services=true

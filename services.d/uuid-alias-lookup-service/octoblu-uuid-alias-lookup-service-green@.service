[Unit]
Description=octoblu-uuid-alias-lookup-service-green
Wants=vulcand.service
After=vulcand.service

[Service]
TimeoutStartSec=300
Restart=always

ExecStartPre=/bin/sh -c '/usr/bin/docker login \
  --username $(/usr/bin/etcdctl get /docker/quay.io/username) \
  --password $(/usr/bin/etcdctl get /docker/quay.io/password) \
  --email $(/usr/bin/etcdctl get /docker/quay.io/email) \
  quay.io'

ExecStartPre=/usr/bin/etcdctl get /octoblu/uuid-alias-lookup-service/green/port || /usr/bin/echo "etcd did not contain /octoblu/uuid-alias-lookup-service/green/port" && exit 1
ExecStartPre=/usr/bin/etcdctl get /octoblu/uuid-alias-lookup-service/green/docker_url || /usr/bin/echo "etcd did not contain /octoblu/uuid-alias-lookup-service/green/docker_url" && exit 1
ExecStartPre=-/usr/bin/docker rm --force octoblu-uuid-alias-lookup-service-green-env-%i octoblu-uuid-alias-lookup-service-green-%i
ExecStartPre=/bin/sh -c "/usr/bin/docker run --rm \
  --name octoblu-uuid-alias-lookup-service-green-env-%i \
  -e ETCDCTL_PEERS=http://$(ifconfig docker0 | grep 'inet ' | awk '{print $2}'):2379 \
  quay.io/octoblu/etcd-to-env /octoblu/uuid-alias-lookup-service/env > /tmp/octoblu-uuid-alias-lookup-service-green.env \
"
ExecStart=/bin/sh -c "/usr/bin/docker run --rm \
  --name octoblu-uuid-alias-lookup-service-green-%i \
  --env-file /tmp/octoblu-uuid-alias-lookup-service-green.env \
  --memory 512m \
  -p $(/usr/bin/etcdctl get /octoblu/uuid-alias-lookup-service/green/port):80 \
  $(/usr/bin/etcdctl get /octoblu/uuid-alias-lookup-service/green/docker_url) \
"
ExecStop=/usr/bin/docker stop --time=5 octoblu-uuid-alias-lookup-service-green-%i

[Install]
WantedBy=multi-user.target

[X-Fleet]
Conflicts=octoblu-uuid-alias-lookup-service-green@*.service
MachineMetadata=Services=true

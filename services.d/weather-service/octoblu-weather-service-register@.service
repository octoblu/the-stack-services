[Unit]
Description=octoblu-weather-service-register
After=octoblu-weather-service@%i.service

[Service]
TimeoutStartSec=300
EnvironmentFile=/etc/environment
Restart=always
ExecStartPre=/bin/bash -c " \
  echo 'Writing kronk to /tmp' \
  && /usr/bin/etcdctl get /kronk/register/start-pre > /tmp/octoblu-weather-service-register-%i-kronk-start-pre \
  && /usr/bin/etcdctl get /kronk/register/start > /tmp/octoblu-weather-service-register-%i-kronk-start \
  && /usr/bin/etcdctl get /kronk/register/stop > /tmp/octoblu-weather-service-register-%i-kronk-stop \
  && chmod +x /tmp/octoblu-weather-service-register-%i-kronk-* \
"
ExecStartPre=/bin/bash -c " \
  /tmp/octoblu-weather-service-register-%i-kronk-start-pre \
    octoblu-weather-service \
    %i \
    /octoblu/weather-service/register-env \
"
ExecStart=/bin/sh -c " \
  /tmp/octoblu-weather-service-register-%i-kronk-start \
    octoblu-weather-service \
    %i \
"
ExecStop=/bin/sh -c " \
  /tmp/octoblu-weather-service-register-%i-kronk-stop \
    octoblu-weather-service \
    %i \
"

[X-Fleet]
X-ConditionMachineOf=octoblu-weather-service@%i.service
